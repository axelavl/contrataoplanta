# ============================================================
#  contrataoplanta.cl — requirements.txt
#  Python 3.11+
# ============================================================

# ── HTTP async y scraping ───────────────────────────────────
# aiohttp 3.14.1: cierra CVE-2026-34993/47265/50269 y la familia
# CVE-2026-54273..54280 (sobre 3.13.5). API async compatible.
aiohttp==3.14.1
beautifulsoup4==4.12.3
# lxml 6.1.1: fix de PYSEC-2026-87 (la serie 5.x no tiene arreglo).
# Compatible con el parser de BeautifulSoup y la extracción del proyecto.
lxml==6.1.1
requests==2.33.1
# pdfplumber 0.11.9 arrastra pdfminer.six con fix de CVE-2025-64512
# y CVE-2025-70559.
pdfplumber==0.11.9
# OCR opcional (scrapers/carabineros.py --ocr para las Bases escaneadas).
# Requiere además el binario del sistema `tesseract-ocr` con `tesseract-ocr-spa`.
# Descomentar si se habilita OCR en el deploy:
# pytesseract==0.3.13
# Playwright: navegador headless para scrapers de sitios JS/anti-bot
# (bcentral=Incapsula, laborum=SPA Navent). OJO: `pip install` instala sólo la
# librería; el navegador hay que bajarlo aparte en el deploy con:
#   python -m playwright install --with-deps chromium
playwright==1.60.0

# ── Base de datos ───────────────────────────────────────────
psycopg2-binary==2.9.9
# SQLAlchemy ya se usaba vía `db/database.py`; ahora se pinnea aquí para
# que Alembic pueda importar el metadata en las migraciones.
SQLAlchemy==2.0.30
alembic==1.13.3

# ── API FastAPI ─────────────────────────────────────────────
# starlette 1.3.1 cierra PYSEC-2026-161 y CVE-2026-48817/48818/54282/54283.
# El fix solo existe en la serie 1.x, que exige fastapi>=0.126: por eso se
# sube fastapi a 0.138.1 (acepta starlette<2.0). pydantic 2.9.2 sigue válido.
# OJO despliegue: starlette 1.x mueve TestClient a `httpx2` (solo afecta tests
# que usen TestClient, no a uvicorn en producción).
fastapi==0.138.1
starlette==1.3.1
uvicorn[standard]==0.32.0
pydantic[email]==2.9.2
slowapi==0.1.9
# PyJWT 2.13.0: fix de PYSEC-2026-175..179.
PyJWT==2.13.0

# ── Configuración ───────────────────────────────────────────
# python-dotenv 1.2.2: fix de CVE-2026-28684.
python-dotenv==1.2.2

# ── Utilidades de datos ─────────────────────────────────────
python-dateutil==2.9.0
openpyxl==3.1.5
streamlit==1.57.0

# ── Emails transaccionales ──────────────────────────────────
# resend 0.8.0 pineaba requests<2.32 y entraba en conflicto con el
# upgrade a requests==2.32.3 (CVE-2024-35195). 2.4.0 mantiene la misma
# API pública (`resend.Emails.send({...})`, `resend.api_key = ...`) y
# sólo pide requests>=2.31.0 — compatible.
resend==2.4.0

# ── Búsqueda rápida (Meilisearch) ──────────────────────────
meilisearch==0.31.6

# ── Scheduling opcional ─────────────────────────────────────
apscheduler==3.10.4

# ── Observabilidad ──────────────────────────────────────────
rich==13.7.1

# ── Generación de imágenes OG/Twitter por oferta ────────────
# Pillow 12.2.0: fix CVE-2026-25990 (fuzzing) y CVE-2026-40192.
Pillow==12.2.0
